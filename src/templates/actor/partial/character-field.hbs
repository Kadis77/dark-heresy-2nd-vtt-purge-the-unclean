<div class="dh-header__item-wrapper--{{cleanFieldName key}}">
    <span class="dh-header__title">{{capitalize key}}</span>
    {{#if options}}
        <select class="dh-header__select acolyte-{{key}}" name="system.bio.{{key}}">
            <option value=""></option>
            {{selectOptions (arrayToObject options) selected=value}}
        </select>
    {{else}}
        <input class="dh-header__input" name="system.bio.{{key}}" type="text" value="{{value}}" />
    {{/if}}
</div>
